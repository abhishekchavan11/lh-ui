import { RawHtmlDirective } from './raw-html.directive';

describe('RawHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new RawHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
